{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ with .Description }}{{ . }}{{ else }}{{ .Site.Params.description }}{{ end }}</description>
    <generator>Hugo -- gohugo.io</generator>
    {{- with .Site.LanguageCode }}
    <language>{{.}}</language>
    {{- end }}
    {{- with .Site.Author.name }}
    <managingEditor>{{ $.Site.Author.email }}{{ with . }} ({{.}}){{end}}</managingEditor>
    {{- end }}
    {{- with .Site.Author.name }}
    <webMaster>{{ $.Site.Author.email }}{{ with . }} ({{.}}){{end}}</webMaster>
    {{- end }}
    {{- with .Site.Copyright }}
    <copyright>{{.}}</copyright>
    {{- end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- with .OutputFormats.Get "RSS" }}
    <atom:link href="{{ .Permalink }}" rel="self" type="{{ .MediaType }}" />
    {{- end }}
    {{- range $pages }}
    {{- $image := "" -}}
    {{- with .Params.image -}}
      {{- $image = . | absURL -}}
    {{- else -}}
      {{- $content := .Content -}}
      {{- $imgRegex := `<img[^>]+src="([^">]+)"` -}}
      {{- if findRE $imgRegex $content 1 -}}
        {{- $matches := findRE $imgRegex $content 1 -}}
        {{- with index $matches 0 -}}
          {{- $srcRegex := `src="([^">]+)"` -}}
          {{- $srcMatch := findRE $srcRegex . 1 -}}
          {{- with index $srcMatch 0 -}}
            {{- $src := replaceRE `src="([^">]+)"` "$1" . -}}
            {{- if hasPrefix $src "http" -}}
              {{- $image = $src -}}
            {{- else -}}
              {{- $image = $src | absURL -}}
            {{- end -}}
          {{- end -}}
        {{- end -}}
      {{- end -}}
    {{- end }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with .Site.Author.email }}
      <author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>
      {{- end }}
      <guid>{{ .Permalink }}</guid>
      <description><![CDATA[{{ .Summary | html }}]]></description>
      {{- with $image }}
      <enclosure url="{{ . }}" type="image/jpeg" />
      <media:content url="{{ . }}" medium="image" />
      {{- end }}
    </item>
    {{- end }}
  </channel>
</rss>